class: ShoebillTemplateBaseline
table: shoebill_template_baselines
fields:
    id: UuidValue, defaultModel=random_v7

    # The language the pdf was originally created in.
    referenceLanguage: SupportedLanguages

    createdAt: DateTime, default=now

    version: ShoebillTemplateVersion?, relation(name=version_baseline_relation)

    pdfImplementationsPayloads: List<ShoebillTemplateVersionImplementation>?, relation(name=baseline_implementation_relation)
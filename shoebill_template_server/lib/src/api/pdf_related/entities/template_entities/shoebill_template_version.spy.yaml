class: ShoebillTemplateVersion
table: shoebill_template_versions
fields:
    createdAt: DateTime, default=now
    schema: SchemaDefinition?, relation
    inputId: int
    input: ShoebillTemplateVersionInput?, relation(name=shoebill_template_version_input_relation, field=inputId)
    scaffold: ShoebillTemplateScaffold?, relation(name=scaffold_to_versions)
    implementations: List<ShoebillTemplateVersionImplementation>?, relation(name=version_implementation_relation)

indexes:
    template_input_unique_index:
        fields: inputId
        unique: true  
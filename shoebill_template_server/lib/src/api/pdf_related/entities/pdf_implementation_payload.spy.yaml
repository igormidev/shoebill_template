class: PdfImplementationPayload
table: pdf_implementation_payload
fields:
    pdfId: UuidValue
    stringifiedJson: String
    language: SupportedLanguages, defaultModel=english
    createdAt: DateTime, default=now
indexes:
    pdf_id_language_unique:
        fields: pdfId, language
        unique: true
    pdf_id_idx:
        fields: pdfId